import styled from "styled-components";

export const StyledNavbar = styled.nav`
  ul {
    margin: 2rem 0;
    display: flex;
    flex-flow: row wrap;
    justify-content: flex-end;
    align-items: center;
    li {
      list-style: none;
    }
  }
`;
